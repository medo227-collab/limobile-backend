services:
  - type: web
    name: limobile-backend
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 60 src.main:app
    envVars:
      - key: FLASK_ENV
        value: production
